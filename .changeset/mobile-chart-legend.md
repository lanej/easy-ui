---
"@easypost/easy-ui": minor
---

Give ordinary unpositioned Cartesian charts a wrapping, keyboard-accessible series legend outside the plot, with 44px touch targets and room for axis labels. Preserve explicit native layouts and controlled legend selection; expose `layout="native"` and the reusable `ChartLegend` for application composition.
