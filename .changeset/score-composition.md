---
"@easypost/easy-ui": minor
---

Add ScoreComposition and reusable score primitives for accessible explanations of signals, capped contributions, and supplied results. Includes responsive DOM layout, decorative SVG connectors, exact values, contribution title disclosures, hover/focus connection emphasis, explicitly untriggered signal paths, explicit sentiment colors with visible status labels, contribution fullness states, and prominent application-owned result decisions without a graph engine.
