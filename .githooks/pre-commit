#!/bin/sh
# Managed by easy-ui: full repository lint before committing.
set -eu

cd "$(git rev-parse --show-toplevel)"
printf '%s\n' 'Checking full repository lint before committing...'
exec npm run lint
